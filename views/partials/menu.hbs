<nav class="navbar navbar-default">
	<div class="container-fluid">
		{{#if loggedIn}}
		<p class="navbar-text navbar-right">Üdv, {{user.forename}}!</p>
		<img src="{{user.avatar}}" alt="{{user.surname}} {{user.forename}}" height="200"></img>
		{{/if}}
		
		{{#if loggedIn}}
		<ul class="nav navbar-nav">
			<li><a href="/list">Listázás</a></li>
			<li><a href="/add">Hozzáadás</a></li>
		</ul>
		{{/if}}
		<ul class="nav navbar-nav navbar-right">
		    {{#if loggedIn}}
		    <li><a href="/auth/logout">Kilépés</a></li>
		    {{/if}}
		</ul>
	</div>
</nav>